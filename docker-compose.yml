services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: etl
      POSTGRES_PASSWORD: etl
      POSTGRES_DB: etl
    ports:
      - "5432:5432"
    volumes:
      # Persist the warehouse across container restarts.
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U etl -d etl"]
      interval: 3s
      timeout: 3s
      retries: 20

  etl:
    build: .
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      HTTP_ADDR: ":8080"
      POLL_INTERVAL: "30s"
      FETCH_RESULTS: "10"
      API_URL: "https://randomuser.me/api/"
      API_TIMEOUT: "15s"
      DATABASE_URL: "postgres://etl:etl@postgres:5432/etl?sslmode=disable"
      DATA_DIR: "/app/data"
      LOG_PATH: "/app/logs/etl.log"
      SOURCE_NAME: "randomuser"
    ports:
      - "8080:8080"
    volumes:
      # Host mounts simulate a durable data lake + log volume.
      # Raw     -> ./data/raw
      # Processed -> ./data/processed
      # Logs    -> ./logs/etl.log
      - ./data:/app/data
      - ./logs:/app/logs

volumes:
  postgres_data:
